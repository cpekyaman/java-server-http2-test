pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = 'java-server-http2-test'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        mavenLocal()
    }
}

